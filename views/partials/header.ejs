<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Trackr</title>
    <link href="/style.css" rel="stylesheet">
</head>
<body>



<div class="nav-bg">
    
    <nav class="nav-wrapper">

        <div class="left-nav-group">
            <ul>
                <li><a href="/" class="<%= page === 'home' ? 'active' : '' %>">Home</a></li>
                <li><a href="/games" class="<%= page === 'games' ? 'active' : '' %>">Games</a></li>
                <% if (user){ %>
                    <li><a href="/profile" class="<%= page === 'profile' ? 'active' : '' %>">Profile</a></li>
                <%} %>
            </ul>

            <div class="nav-search-bar">
                <form class="flex-schrink" method="get" action="/games">
                <input class="input-field-nav" type="text" name="search" placeholder="Search games..." >
                <button class="search-button" type="submit" aria-label="Search">
                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M10 2a8 8 0 0 1 6.32 12.906l4.387 4.387-1.414 1.414-4.387-4.387A8 8 0 1 1 10 2zm0 2a6 6 0 1 0 0 12a6 6 0 0 0 0-12z"/>
                    </svg>
                </button>
                </form>
            </div>
        </div>
        

        

        
        <div class="auth-links right-nav-group">

            
            <% if (user){ %>
                <!-- Welcome message is now inside the new group -->
                <span class="welcome-message">Hello, <%= user.username %>!</span>
            <%} %>

            <% if (user){ %>
                <a class="button login" href="/logout">Logout</a>
            <%} else{ %>
                <a class="button signup" href="/register">Sign Up</a>
                <a class="button login" href="/login">Login</a>
            <%} %>
        </div>
    </nav>
</div>